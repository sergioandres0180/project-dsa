# Fichero alternativo para recrear el entorno del modelo con versiones disponibles en PyPI.
# Estas versiones son estables y cercanas a las especificadas originalmente.
# Si el pickle no carga, se recomienda volver a exportar el modelo con dependencias soportadas.

mlflow==3.6.0
cloudpickle==3.1.0
numpy==2.2.6
pandas==2.2.3
pyarrow==18.1.0
scikit-learn==1.5.2
scipy==1.14.1
